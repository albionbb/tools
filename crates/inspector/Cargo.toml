[package]
name = "inspector"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
photon-decoder = { path = "../photon-decoder" }
albion-packets = { path = "../albion-packets" }
capture = { path = "../capture" }
